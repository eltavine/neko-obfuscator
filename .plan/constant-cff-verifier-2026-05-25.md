# Constant/CFF Verifier Repair - 2026-05-25

## Evidence chain

- The runtime-variable no-leak compatibility gate is blocked by
  `JvmConstantObfuscationIntegrationTest.constantObfuscationCoversJvmNumericShapesWithCff`.
- The recorded failing command in
  `.plan/runtime-variable-no-leak-hardening-2026-05-25.md` used repository
  Gradle with `java.io.tmpdir` redirected inside `build/tmp` and failed while
  writing `ConstantShapes` with `COMPUTE_FRAMES`.
- The recorded verifier diagnostic was
  `AnalyzerException: Expected D, but found .` at generated instruction 7989.
- The failing transform set was `keyDispatch`, `controlFlowFlattening`, and
  `constantObfuscation`; `runtimeVariableObfuscation` was not enabled.
- A fresh pre-RVNL worktree at commit `0e2f534` reproduced the same
  `JvmConstantObfuscationIntegrationTest` failure, proving this blocker
  predates RVNL-2/RVNL-3 and must be fixed as its own generic JVM transform
  defect.
- Fresh focused reproduction on this worktree used
  `env JAVA_TOOL_OPTIONS=-Djava.io.tmpdir=/mnt/d/Code/Security/NekoObfuscator/build/tmp ./gradlew :neko-test:test --tests dev.nekoobfuscator.test.JvmConstantObfuscationIntegrationTest`
  and failed with `BUILD FAILED in 1s`.
- Fresh generated artifacts from that command were
  `build/tmp/neko-test-constants/ConstantShapes.java` timestamp
  `2026-05-25 13:35:26 +0800`, `constant-shapes.jar` timestamp
  `2026-05-25 13:35:26 +0800`, and the failed partial
  `constant-shapes-obf.jar` timestamp `2026-05-25 13:35:27 +0800`.
- Fresh verifier diagnostics identified method `ConstantShapes.arrays(J)J`.
  `JarOutput` reported `Expected D, but found .` at `DLOAD 17`, with the same
  physical local 17 accessed as `ISTORE`/`ILOAD`, `LSTORE`/`LLOAD`,
  `FSTORE`/`FLOAD`, and `DSTORE`/`DLOAD` before the failing `DLOAD 17`.
  The source fixture's enhanced primitive-array loops reuse one verifier local
  slot for sequential `int`, `long`, `float`, and `double` loop variables.
- Failing invariant: CFF synthetic dispatcher and fake/poison paths must not
  rely on javac-style cross-type physical local-slot reuse. Once CFF introduces
  non-source dispatch joins, each protected verifier local slot must have a
  stable compatible verification shape across all synthetic paths that can
  reach a block. A block that executes `DLOAD n` must not be reachable with
  local `n` as verifier TOP or as a different primitive kind.
- Current source boundary before implementation:
  `JvmConstantObfuscationPass` replaces CFF application numeric constants and
  primitive array literal sequences after CFF metadata is published, and
  `ControlFlowFlatteningPass`/CFF frame grouping owns the verifier locals and
  dispatcher shapes that the replacement must preserve.

## Subtasks

- [x] 1. Plan intake checkpoint.
  - Scope: record this independent high-risk plan, keep it isolated from
    unrelated native/runtime worktree changes, record the active todo
    counterpart, and dispatch subagent plan-intake review before code edits.
  - Required evidence: reviewer confirms the plan is generic, evidence-backed,
    and compliant.
  - Validation target: plan-intake subagent review.
  - Completion criteria: plan-intake review passes and the plan checkpoint is
    committed with `git add -f` for this `.plan/` file before runtime
    implementation starts.
  - Completed evidence: plan-intake subagent review returned PASS with no
    blocking findings.

- [x] 2. Fresh focused reproduction and invariant location.
  - Scope: rerun the focused failing test after permission to use repository
    Gradle, then inspect the fresh generated bytecode and CFF/constant metadata
    to identify the exact method, instruction, expected verifier type, actual
    local/stack value, and transform path that creates the invalid frame.
  - Required evidence: fresh focused Gradle output identifies the failing
    method, instruction/site, and verifier invariant; concrete instruction dump
    and source-path mapping show whether the break is caused by primitive array
    literal replacement, numeric constant replacement, CFF stack spill/local
    restoration, dispatcher frame grouping, or generated helper interaction.
  - Validation command: after explicit permission to use repository
    `./gradlew`, run `env JAVA_TOOL_OPTIONS=-Djava.io.tmpdir=/mnt/d/Code/Security/NekoObfuscator/build/tmp ./gradlew :neko-test:test --tests dev.nekoobfuscator.test.JvmConstantObfuscationIntegrationTest`.
  - Completion criteria: root cause is stated without probability language and
    identifies the failing invariant and responsible generic transform path.
  - Completed evidence: fresh focused validation failed as recorded above.
    The responsible generic path is CFF's verifier-local shape preservation for
    protected methods with mixed-type physical local-slot reuse; constant
    obfuscation exposes the path by keeping the `constantObfuscation` + CFF
    fixture in the compatibility gate, but the failing invariant is the CFF
    synthetic dispatch/local-shape contract.

- [x] 3. Implement the generic verifier-preserving repair.
  - Scope: make one coherent architecture-level change in the responsible JVM
    transform path. The fix must preserve full constant obfuscation coverage,
    full CFF block semantics, dynamic key dependence, and verifier-correct
    wide primitive local/stack shapes.
  - Required evidence: source diff maps directly to the invariant from subtask
    2; no sample-, method-, descriptor-, instruction-number-, or log-specific
    special case is introduced.
  - Validation command: after permission to use repository `./gradlew`, rerun
    the focused `JvmConstantObfuscationIntegrationTest`.
  - Completion criteria: focused test passes from a fresh generated artifact
    and subagent implementation review passes.
  - Completed evidence: CFF now normalizes verifier locals before protected
    dispatch rewriting by splitting only verifier-incompatible primitive/wide
    local-slot shapes, treating all references as one verifier-compatible
    reference category, adding protected-entry defaults for protected-region
    local reads that lack an earlier pre-protected write, and restoring precise
    reference verifier types with post-`ALOAD` `CHECKCAST` where the original
    frame carried a non-`Object` descriptor. The split path does not remap the
    recorded CFF method-key local or its second slot, preserving CFF emissions
    after parameter packing.
  - Completed evidence: final focused/CFF validation used
    `env JAVA_TOOL_OPTIONS=-Djava.io.tmpdir=/mnt/d/Code/Security/NekoObfuscator/build/tmp ./gradlew :neko-test:test --tests dev.nekoobfuscator.test.JvmMethodParameterObfuscationIntegrationTest --tests dev.nekoobfuscator.test.JvmConstantObfuscationIntegrationTest --tests dev.nekoobfuscator.test.ControlFlowFlatteningAlgebraicAuditTest`
    and completed with `BUILD SUCCESSFUL in 4s`.
  - Completed evidence: fresh artifacts included
    `build/tmp/neko-test-constants/constant-shapes-obf.jar` timestamp
    `2026-05-25 13:52:59 +0800`, and running it with
    `java -XX:-UsePerfData -jar build/tmp/neko-test-constants/constant-shapes-obf.jar`
    printed `CONSTANT SHAPES OK`.
  - Completed evidence: CFF audit artifacts
    `build/tmp/neko-test-cff-direct-integrity/cff-audit-shapes-obf.jar`
    timestamp `2026-05-25 13:53:00 +0800` and
    `build/tmp/neko-test-cff-packed-integrity/cff-audit-shapes-obf.jar`
    timestamp `2026-05-25 13:53:01 +0800` were generated by the passing run.
  - Completed evidence: implementation subagent review returned PASS with no
    blocking findings after rejecting and revising an earlier reference
    descriptor split that was not semantics-preserving for valid reference
    merges.

- [x] 4. Compatibility closure and RVNL unblock.
  - Scope: rerun the RVNL compatibility set that was blocked by the constant/CFF
    verifier issue and update the RVNL plan with fresh post-fix evidence.
  - Required evidence: the full compatibility command completes freshly; tests
    still audit no stable primitive mask pair and no runtime-variable reference
    frame; no JNI/JVMTI/native fallback/original-bytecode fallback is
    introduced.
  - Validation command: after permission to use repository `./gradlew`, run
    `env JAVA_TOOL_OPTIONS=-Djava.io.tmpdir=/mnt/d/Code/Security/NekoObfuscator/build/tmp ./gradlew :neko-test:test --tests dev.nekoobfuscator.test.JvmRuntimeVariableObfuscationIntegrationTest --tests dev.nekoobfuscator.test.JvmConstantObfuscationIntegrationTest --tests dev.nekoobfuscator.test.JvmStringObfuscationIntegrationTest --tests dev.nekoobfuscator.test.JvmInvokeDynamicObfuscationIntegrationTest --tests dev.nekoobfuscator.test.JvmMethodParameterObfuscationIntegrationTest`.
  - Completion criteria: compatibility set passes freshly, final subagent plan
    review passes, RVNL subtask 4 is marked complete with fresh evidence, and
    commits remain scoped to each completed subtask.
  - Completed evidence: final RVNL compatibility validation used the command
    above and completed with `BUILD SUCCESSFUL in 3s`.
  - Completed evidence: fresh compatibility artifacts included
    `build/tmp/neko-test-runtime-vars/runtime-variable-shapes-obf.jar`
    timestamp `2026-05-25 13:58:02 +0800`,
    `build/tmp/neko-test-constants/constant-shapes-obf.jar` timestamp
    `2026-05-25 13:58:00 +0800`,
    `build/tmp/neko-test-method-parameters/parameter-shapes-obf.jar` timestamp
    `2026-05-25 13:58:01 +0800`,
    `build/tmp/neko-test-strings/string-shapes-obf.jar` timestamp
    `2026-05-25 13:58:03 +0800`, and
    `build/tmp/neko-test-indy-reference/indy-reference-shapes-obf.jar`
    timestamp `2026-05-25 13:58:01 +0800`.
  - Completed evidence: scoped test-result inspection of the five compatibility
    XML files found no `<failure>` or `<error>` entries.

## Constraints

- No sample-, benchmark-, class-, method-, descriptor-, instruction-number-, or
  log-string-specific fix.
- No CFF coverage weakening, block-boundary reduction, static dispatch
  replacement, dead key flow, or self-canceling key logic.
- No fallback to `COMPUTE_MAXS`, `-noverify`, original bytecode, skip-on-error,
  JNI, JVMTI, or native helper runtime.
- Do not use stale generated artifacts as proof; every completion claim needs a
  fresh post-edit artifact and command output.
- Do not create files or folders under `/tmp`.
