# NekoObfuscator JVM Obfuscation TODO

Status legend:
- `[ ]` not completed / not verified
- `[-]` actively in progress only while the current implementation is underway
- `[x]` completed only after the listed artifact was regenerated and run

## Hard Constraints

- No bug-specific, sample-specific, benchmark-specific, class-specific, method-specific, owner/name-specific, descriptor-specific, or crash-site-specific repair is allowed. If a change only explains one observed failure instead of a plan-level category, stop and redesign it as a generic mechanism.
- Do not mark any task complete unless the exact requested artifact is regenerated and run after the latest edit.
- Use repository `./gradlew`; do not create or use temporary `gradlew`.
- Before editing code for a new subtask, record the runtime target(s) that will prove the changed path.
- Compile-only, unit-only, source-only, or stale generated jar checks are not enough for `[x]`.
- After each TODO item is completed and verified, commit the completed slice before starting the next TODO item.

## Runtime Target Matrix

- `J-build`: `./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
- `J-number-xor`: regenerate a JVM-only obfuscated test jar with `numberEncryption.algorithm=xor`, run it with `java -jar`, and inspect the output for `ALL TESTS PASSED`.
- `J-number-aes`: regenerate a JVM-only obfuscated test jar with `numberEncryption.algorithm=aes`, run it with `java -jar`, and inspect the output for `ALL TESTS PASSED`.
- `J-inspect`: inspect regenerated bytecode/logs for the changed path: no `bsmNumber`/number `invokedynamic` for `xor`/`aes`, numeric float/double/int/long sites are transformed where eligible, and runtime helper classes appear only when required.

## Stage 1: Number Constants And Dynamic Keys

- [x] J1.1 Add configurable number encryption algorithms: fast `xor`, one-time cached `aes`, and legacy `indy`; cover int, long, float, and double without requiring per-use invokedynamic on `xor`/`aes`.
  - Runtime targets before editing: `J-build`, `J-number-xor`, `J-number-aes`, `J-inspect`.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated `build/jvm-obf-verify/out/numeric-xor.jar` and ran `java -jar`, output included `NUMERIC OK`
    - regenerated `build/jvm-obf-verify/out/numeric-aes.jar` and ran `java -jar`, output included `NUMERIC OK`
    - `javap` inspection: `xor bsmNumber hits: 0`, `aes bsmNumber hits: 0`, `aes cached fields: 24`, `aes runtime class entries: 1`, `xor runtime class entries: 0`
- [x] J1.2 Add class-local key dispatcher depth/shape options that can feed number/string/invoke metadata keys without a single universal pattern.
  - Runtime targets before editing: `J-build`; regenerate and run JVM-only jars with `numberEncryption.keyDispatcher`, `stringEncryption.keyDispatcher`, and `invokeDynamic.keyDispatcher`; inspect bytecode for per-class `__neko_k*` methods and successful output.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-keydispatch-verify/out/test-number-kd.jar`, output included `ALL TESTS PASSED`, `javap` found `number dispatcher methods: 20`
    - regenerated and ran `build/jvm-keydispatch-verify/out/test-string-kd.jar`, output included `ALL TESTS PASSED`, `javap` found `string dispatcher methods: 1`
    - regenerated and ran `build/jvm-keydispatch-verify/out/test-invoke-kd.jar`, output included `ALL TESTS PASSED`, `javap` found `invoke dispatcher methods: 1`
- [x] J1.3 Thread control-flow dynamic keys into number encryption without requiring one invokedynamic per constant.
  - Runtime targets before editing: `J-build`; regenerate and run a JVM-only jar with `controlFlowFlattening` + `numberEncryption.useControlFlowKey=true`; inspect bytecode for `NekoContext.flowKey` and no `bsmNumber`.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-flow-number-verify/out/test-cff-number-flow.jar`, output included `ALL TESTS PASSED`
    - `javap` inspection found `flowKey calls: 9`, `bsmNumber hits: 0`
    - obfuscation log had `COMPUTE_FRAMES fallback lines: 0`

## Stage 2: Control Flow

- [x] J2.1 Add dispatcher depth option for nested switch dispatch while preserving verifier correctness.
  - Runtime targets before editing: `J-build`; plus regenerate and run a JVM-only test jar with `controlFlowFlattening.dispatcherDepth=2`, then inspect `javap` for nested `lookupswitch`.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated `build/jvm-cff-verify/out/test-sample-cff-depth.jar` with `dispatcherDepth=2`
    - `java -jar build/jvm-cff-verify/out/test-sample-cff-depth.jar` output included `ALL TESTS PASSED`
    - `javap` inspection found `lookupswitch count: 10`
    - obfuscation log had `COMPUTE_FRAMES fallback lines: 0`
- [x] J2.2 Add per-method/per-class dispatcher shape variation and arithmetic state encoders.
  - Runtime targets before editing: `J-build`; regenerate and run JVM-only CFF jars with `dispatcherDepth=4` and shape variation enabled; inspect bytecode for nested switch plus mixed arithmetic bucket expressions and no frame fallback.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-cff-shape-verify/out/test-cff-shape.jar`, output included `ALL TESTS PASSED`
    - `javap` inspection found `lookupswitch count: 14`, `Integer.rotateRight count: 6`, `ixor count: 91`
    - obfuscation log had `COMPUTE_FRAMES fallback lines: 0`

## Stage 3: InvokeDynamic

- [x] J3.1 Remove leaked callsite names from invoke obfuscation; keep only encrypted metadata and key entry material.
  - Runtime targets before editing: `J-build`; regenerate and run JVM-only `invokeDynamic` jar; inspect bytecode/constant pool so indy callsite names are opaque and metadata fields no longer use owner/name/desc suffixes.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-invoke-opaque-verify/out/test-invoke-opaque.jar`, output included `ALL TESTS PASSED`
    - `javap -v` inspection found transformed invoke callsite names like `_1x1rcsb`, `semantic indy name hits: 0`, `semantic metadata suffix hits: 0`, and numeric metadata fields like `__i0_0`
- [x] J3.2 Reduce invokeDynamic steady-state overhead where the target can be cached or converted to a direct wrapper.
  - Runtime targets before editing: `J-build`; regenerate and run JVM-only `invokeDynamic` jar with non-void transformed calls; inspect runtime bootstrap bytecode/source path for direct `asSpreader` callsite and successful output.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-invoke-direct-verify/out/test-invoke-direct.jar`, output included `ALL TESTS PASSED`
    - source/path inspection found `asSpreader source hits: 1`; legacy `invokeWithArguments` remains only for void fallback

## Stage 4: Outliner

- [x] J4.1 Redesign outliner around verifier-safe stack/local frame analysis instead of int-only local assumptions.
  - Runtime targets before editing: `J-build`; regenerate and run JVM-only jar with `controlFlowFlattening` + `outliner`; inspect bytecode for `__neko_o*` methods using typed descriptors beyond int-only assumptions and no frame fallback.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-outliner-verify/out/test-outliner-only.jar`, output preserved `Remainder 2`, had `outlined methods: 6`, and had no frame fallback
    - regenerated and ran `build/jvm-outliner-typed-verify/out/typed-outliner.jar`, output included `OUTLINER TYPED OK`, `javap` found `outlined methods: 4`, `typed descriptors: 2`, and no frame fallback
    - regenerated and ran `build/jvm-outliner-typed-verify/out/typed-cff-outliner.jar`, output included `OUTLINER TYPED OK` and no frame fallback
- [x] J4.2 Validate outliner with try/catch, primitive widths, references, and control-flow transformed methods.
  - Runtime targets before editing: add integration coverage, run `J-build`, and regenerate/run typed outliner artifacts covering references, long/double, try/catch, and CFF+outliner.
  - Verified 2026-05-03:
    - added `ObfuscationIntegrationTest.testOutlinerHandlesTypedLocalsAndControlFlowOutput`
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - test covers reference locals, `long`, `double`, try/catch runtime output, generated `__neko_o*`, and a CFF+outliner runtime jar

## Stage 5: Renamer

- [x] J5.1 Implement early `RenamerPass` with class/method/field/package mapping and resource/manifest updates.
  - Runtime targets before editing: `J-build`; regenerate and run JVM-only `renamer` jar; inspect jar entries, manifest `Main-Class`, and member names.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-renamer-verify/out/test-renamed.jar`, output included `ALL TESTS PASSED`
    - inspection found `old class entries: 0`, `new class entries: 1`, `manifest main a: 1`, `old member names: 0`
- [x] J5.2 Preserve reflection-sensitive classes/members through rules, annotations, native targets, entrypoints, and runtime references.
  - Runtime targets before editing: add rule/reflection preservation checks; run `J-build`; regenerate and run renamer jar containing `Class.forName` string references and rule-excluded classes.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-renamer-preserve-verify/out/reflect-renamed.jar`, output included `RENAMER PRESERVE OK`
    - inspection found `KeepMe entry: 1` for `Class.forName("KeepMe")`, `RuleKeep entry: 1` for rule exclusion, `RenameMe old entry: 0`, and `short renamed entries: 2`

## Stage 6: Full JVM Artifact Hardening

- [x] J6.1 Stop generated string/invoke scaffolding from being re-obfuscated by `NumberEncryption`, add per-bucket key dispatcher math variants, and keep runtime-key opaque predicates verifier-safe.
  - Runtime targets before editing: `J-build`; regenerate exact `/home/fadouse/Desktop/TEST.jar` from the JVM-only all-obfuscation config with native disabled; run `java -jar /home/fadouse/Desktop/TEST.jar`; inspect `javap` for `r/a.b` string decrypt arguments using a single class-local key dispatcher call rather than nested dispatcher calls, try/catch methods containing `invokedynamic` and nested `lookupswitch`, no runtime API symbol leak, and no `COMPUTE_FRAMES` fallback.
  - Verified 2026-05-03:
    - `bash ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated `/home/fadouse/Desktop/TEST.jar` with native disabled and ran `java -jar /home/fadouse/Desktop/TEST.jar`, output included `ALL TESTS PASSED`
    - `javap` inspection found `r/a.b` callsites shaped as class constant + field index constant + one `__neko_k*` key dispatch, not nested `__neko_k*` arguments
    - `javap` inspection of `e()` found nested `lookupswitch` and `invokedynamic` in the try/catch method
    - runtime symbol inspection found no `dev/nekoobfuscator/runtime`, `NekoBootstrap`, `NekoContext`, `NekoKeyDerivation`, `decryptString`, `bsmInvoke`, or native library entries in `/home/fadouse/Desktop/TEST.jar`
    - obfuscation log had `COMPUTE_FRAMES fallback lines: 0`

- [x] J6.2 Replace global runtime stack/key noise with local state obfuscation, cover constructor call sites after super/this init, and emit a usable mapping artifact for reverse-performance inspection.
  - Runtime targets before editing: `J-build`; regenerate exact JVM-only `/home/fadouse/Desktop/TEST.jar`, `/home/fadouse/Desktop/SnakeGame.jar`, and `/home/fadouse/Desktop/obfusjack-test21.jar`; run all three outputs; inspect regenerated bytecode for constructor `invokedynamic`, constructor/local `lookupswitch`, no StackObfuscation-sourced `r.c.d/e` + `r.f.c` wrapper spam around ordinary statements, generated `.map` files, and no frame fallback/rejection log lines.
  - Current strengthening scope:
    - Add strict JVM coverage accounting and fail-closed jar writing.
    - Replace whole-method skip/original-bytecode behavior with full or verifier-safe tiers for CFF, number, string, invoke, stack, and advanced JVM passes.
    - Verify `test-jars/TEST.jar`, `test-jars/obfusjack-test21.jar`, and `test-jars/SnakeGame.jar` under the full JVM config with Recaf/ASM inspection.
  - Verified 2026-05-09:
    - `JAVA_TOOL_OPTIONS="-Djava.io.tmpdir=$PWD/build/tmp" ./gradlew :neko-cli:installDist :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`
    - regenerated and ran `build/jvm-full-verify/out/TEST-full.jar` from `test-jars/TEST.jar`; output preserved the original self-test status with Basics/Reflects passing and the pre-existing Sec ERROR unchanged.
    - regenerated and ran `build/jvm-full-verify/out/obfusjack-full.jar` from `test-jars/obfusjack-test21.jar`; output reached `=== All tests completed ===` and preserved enum names.
    - regenerated and ran `build/jvm-full-verify/out/SnakeGame-full.jar` from `test-jars/SnakeGame.jar`; process exited successfully.
    - generated mapping artifacts: `TEST-full.jar.map`, `obfusjack-full.jar.map`, and `SnakeGame-full.jar.map`.
    - bytecode inspection found constructor `invokedynamic`, constructor/local switch dispatch, and `wrapper_markers=0` for `r.c.d/e` + `r.f.c` wrapper spam across all three regenerated jars.
    - runtime-symbol scan found no `dev/nekoobfuscator/runtime`, `NekoBootstrap`, `NekoContext`, `NekoKeyDerivation`, `NekoStringDecryptor`, `bsmInvoke`, `bsmString`, or `bsmNumber` strings in the regenerated jars.
    - obfuscation logs had no `COMPUTE_MAXS`, `-noverify`, fallback, verifier rejection, strict coverage failure, or fail-closed markers.

- [-] J6.3 Make full JVM CFF fail closed instead of emitting safe-tier fallback gates, and require string/invoke metadata keys to consume the live CFF flow key when `useControlFlowKey=true`.
  - Runtime targets before editing: `J-build`; regenerate `test-jars/TEST.jar`, `test-jars/obfusjack-test21.jar`, and `test-jars/SnakeGame.jar` with `test-jars/full-obfuscation.yml`; run all three outputs; inspect regenerated bytecode/logs for no minimal CFF gate fallback, no constructor-only gate fallback, no frame-reject fallback, no static-only string/invoke metadata key when CFF flow keys exist, and full CFF hard abort on unsupported methods instead of writing a weakened jar.
  - Current validation blocker recorded 2026-05-09: `J-build` passes with `JAVA_TOOL_OPTIONS="-Djava.io.tmpdir=$PWD/build/tmp -XX:-UsePerfData" ./gradlew :neko-test:test --tests dev.nekoobfuscator.test.ObfuscationIntegrationTest`. Strict full regeneration now exits non-zero and deletes stale output jars/maps for all three full targets, but fails closed at constructors because the current CFF implementation has no verifier-safe uninitialized-this-aware constructor state machine: `TEST.jar` -> `a.<init>()V`, `obfusjack-test21.jar` -> `b.<init>()V`, `SnakeGame.jar` -> `a.<init>(I)V`. Next generic prerequisite is constructor-region CFF that preserves JVM uninitialized-this rules while flattening the post-init constructor body, or a proof-level redesign that can flatten the whole constructor without illegal uninitialized value control flow.
